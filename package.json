{
	"name": "llama3-tokenizer-count",
	"version": "1.0.0",
	"description": "Count tokens using llama3 tokenizer",
	"main": "dist/index.js",
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "tsc",
		"start": "node dist/index.js",
		"build:start": "npm run build && npm run start"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@xenova/transformers": "^2.17.1"
	}
}
